# this is bootstart properties file
# application properties will be taken from config.uri location
spring.application.name=reservation-service
spring.cloud.config.uri=http://localhost:8888
# of via environment variable
#spring.cloud.config.uri=${MY_CONFIG_SERVER_URL}
